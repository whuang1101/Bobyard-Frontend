import React from 'react'
import ReactDOM from 'react-dom/client'
import CommentBoard from './Bobyard.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CommentBoard />
  </React.StrictMode>,
)
